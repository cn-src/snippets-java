plugins {
    id 'java-library'
}

dependencies {
    api platform(project(':snippets-platform'))
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}